{
  "name": "pinktrip-travel-planner",
  "version": "1.0.0",
  "private": true,
  "description": "PinkTrip - a JSON-persisted travel planning demo",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "build": "node --check src/server.js && node --check public/app.js",
    "lint": "node --check src/server.js && node --check public/app.js",
    "reset-data": "node scripts/reset-data.js",
    "test": "node --test test/*.test.js"
  },
  "engines": { "node": ">=20" }
}
